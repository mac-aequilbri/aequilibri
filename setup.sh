#!/bin/bash
# NOTE: This script is for Linux/Mac development only.
# For Windows Server (AWS EC2), use install.ps1 instead.
# See INSTALL.md for the full deployment guide.
echo "For Windows Server deployment, use install.ps1 — see INSTALL.md"
