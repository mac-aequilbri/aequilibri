Django>=4.2,<5.0
anthropic>=0.25.0
Pillow>=10.0.0

# Production WSGI server
# - Gunicorn is used on Render / Linux
# - Waitress is kept for local Windows development
gunicorn>=21.2.0
waitress>=3.0.0

# Serve static files without a separate web server
whitenoise>=6.6.0

# Load .env file into environment (local dev)
python-dotenv>=1.0.0

# PostgreSQL support (Render managed database)
dj-database-url>=2.1.0
psycopg2-binary>=2.9.9
